import React from 'react';

import { Button, Container, Input } from './LoginStyle';

const Login = () => {
  return (
    <Container>
      <Input placeholder="hello" />
      <Input placeholder="hello" />
      <Button>Button</Button>
    </Container>
  );
};

export default Login;
